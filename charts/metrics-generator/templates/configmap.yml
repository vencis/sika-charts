apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  labels:
    release: {{ .Release.Name }}
data:
  config.yml: |
{{ required ".Values.config_yml is required" .Values.config_yml | indent 4 }}
