apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  labels:
    release: {{ .Release.Name }}
data:
  prometheus.yml: |
{{ required ".Values.prometheus_yml is required" .Values.prometheus_yml | indent 4 }}
